import { TownMapScreen } from '@/components/map/TownMapScreen';

/** Standing in the town rather than in a building. The front door of the game. */
export default function Page() {
  return <TownMapScreen />;
}
